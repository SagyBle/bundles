<p>Sagy Blechessss</p>

<!-- ✅ Checkout Button -->
<button id="checkout-button" onclick="testProxy()" style="
  padding: 10px 20px;
  background-color: #008060;
  color: white;
  border: none;
  cursor: pointer;
  margin-top: 10px;
  font-size: 16px;
">
  Test
  
</button>
<button id="checkout-button" onclick="createProductProxy()" style="
  padding: 10px 20px;
  background-color: #008060;
  color: white;
  border: none;
  cursor: pointer;
  margin-top: 10px;
  font-size: 16px;
">
  Create Product
  
</button>
<button id="checkout-button" onclick="createBundle()" style="
  padding: 10px 20px;
  background-color: #008060;
  color: white;
  border: none;
  cursor: pointer;
  margin-top: 10px;
  font-size: 16px;
">
  Create Bundle
  
</button>


<!-- ✅ JavaScript for CORS Fixed Requests -->
<script>
  const createProductProxy = async () => {
    console.log("🚀 Sending request...");

    const requestBody = {
      message: "Hello from Shopify Extension!",
      user: {
        id: "12345",
        name: "Sagy Blecher",
      }
    };

    try {
      const response = await fetch("https://sagy-test-academy.myshopify.com/apps/checkout-proxy/productspublic", {
        method: "POST",
        headers: {
          "Content-Type": "application/json",
          'Access-Control-Allow-Origin': '*'
        },
        body: JSON.stringify(requestBody),
      });

      if (!response.ok) {
        throw new Error(`Request failed: ${response.status}`);
      }

      const data = await response.json();
      console.log("✅ Response:", data);
      alert(`✅ Success: ${data.message}`);
    } catch (error) {
      console.error("❌ Fetch Error:", error);
      alert(`❌ Error: ${error.message}`);
    }
  };
  const testProxy = async () => {
    console.log("🚀 Sending request...");

    const requestBody = {
      message: "Hello from Shopify Extension!",
      user: {
        id: "12345",
        name: "Sagy Blecher",
      }
    };

    try {
      const response = await fetch("https://sagy-test-academy.myshopify.com/apps/checkout-proxy/test", {
        method: "POST",
        headers: {
          "Content-Type": "application/json",
        },
        body: JSON.stringify(requestBody),
      });

      if (!response.ok) {
        throw new Error(`Request failed: ${response.status}`);
      }

      const data = await response.json();
      console.log("✅ Response:", data);
      alert(`✅ Success: ${data.message}`);
    } catch (error) {
      console.error("❌ Fetch Error:", error);
      alert(`❌ Error: ${error.message}`);
    }
  };
  const createBundle = async () => {
    console.log("🚀 Sending create bundle request...");

    // const requestBody = {
    //   message: "Hello from Shopify Extension!",
    //   user: {
    //     id: "12345",
    //     name: "Sagy Blecher",
    //   }
    // };

    try {
      const response = await fetch("https://sagy-test-academy.myshopify.com/apps/checkout-proxy/bundlespublic", {
        method: "POST",
        headers: {
          "Content-Type": "application/json",
          'Access-Control-Allow-Origin': '*'
        },
        body: JSON.stringify({}),
      });

      if (!response.ok) {
        throw new Error(`Request failed: ${response.status}`);
      }

      const data = await response.json();
      console.log("✅ Response:", data);
      alert(`✅ Success: ${data.message}`);
    } catch (error) {
      console.error("❌ Fetch Error:", error);
      alert(`❌ Error: ${error.message}`);
    }
  };
  
  

</script>

{% schema %}
{
  "name": "Checkout Sagy",
  "target": "section",
  "settings": [
    { "type": "product", "id": "product", "label": "Product", "autofill": true },
    { "type": "color", "id": "colour", "label": "Star Colour", "default": "#ff0000" }
  ]
}
{% endschema %}
